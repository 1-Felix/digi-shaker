#pragma once

// Copy this file to wifi_credentials.h and fill in your Wi-Fi credentials.
// wifi_credentials.h is excluded from version control via .gitignore.

#define WIFI_SSID "your-ssid"
#define WIFI_PASSWORD "your-password"
