{
  "name": "digi-shaker-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --watch server/index.ts",
    "build:ui": "cd ui && bun run build",
    "build": "bun run build:ui",
    "start": "bun run server/index.ts"
  },
  "dependencies": {
    "hono": "latest"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
